import PropTypes from 'prop-types';
import React from 'react';

const UserReactions = () => {
  return(
  <div>
      <h3>Hello Reactions</h3>
  </div>
  )
};

UserReactions.propTypes = {
//   name: PropTypes.string.isRequired,
//   updateName: PropTypes.func.isRequired,
};

export default UserReactions;