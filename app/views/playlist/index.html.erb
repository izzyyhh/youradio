[
<% playlist = Server.find(params[:server_id]).playlist %>
<% playlist.tracks.each do |t| %>
    {
        "uri": "<%= t.uri %>",
        "starttime": "<%= t.starttime.to_i %>",
        "duration": "<%= t.duration %>",
        "title": "<%= t.title %>",
        "thumbnail": "<%= t.thumbnail %>",
        "channeltitle": "<%= t.channeltitle %>"
    }
    <% if t != playlist.tracks.last %>
    ,
    <% end %>
<% end %>
]